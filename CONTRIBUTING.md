# 贡献指南

感谢您对 NSBP 的关注！我们欢迎各种形式的贡献，包括但不限于：

- 🐛 报告 Bug
- 💡 讨论新功能的想法
- 📚 改进文档
- ✍️ 提交代码
- 🔧 修复 Bug
- ✨ 添加新功能

## 🚀 快速开始

### 1. Fork 和克隆

```bash
# Fork NSBP 仓库到您的 GitHub 账户

# 克隆您的 fork
git clone https://github.com/YOUR_USERNAME/nsbp.git
cd nsbp

# 添加上游仓库
git remote add upstream https://github.com/erishen/nsbp.git
```

### 2. 安装依赖

```bash
pnpm install
```

### 3. 初始化 Git Hooks

```bash
pnpm run prepare
```

### 4. 启动开发环境

```bash
# 本地开发
pnpm run dev

# 或使用 Docker
make dev
```

### 5. 创建功能分支

```bash
# 同步最新代码
git fetch upstream
git checkout main
git merge upstream/main

# 创建新分支
git checkout -b feature/your-feature-name
# 或
git checkout -b fix/your-bug-fix
```

## 📋 代码规范

### 命名约定

分支命名：
- `feature/feature-name` - 新功能
- `fix/bug-name` - Bug 修复
- `docs/update-docs` - 文档更新
- `refactor/code-refactor` - 代码重构
- `test/add-tests` - 添加测试

提交信息格式（遵循 [Conventional Commits](https://www.conventionalcommits.org/)）：
```
<type>(<scope>): <subject>

<body>

<footer>
```

类型：
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构（既不是新功能也不是修复）
- `test`: 添加测试
- `chore`: 构建或辅助工具的变动
- `perf`: 性能优化
- `ci`: CI 配置文件和脚本的变动

示例：
```
feat(components): add new Button component
fix(api): handle 404 error correctly
docs(readme): update installation instructions
```

### 代码风格

项目使用以下工具来确保代码质量：

- **ESLint**: 代码质量检查
- **Prettier**: 代码格式化
- **Husky**: Git hooks 自动化

在提交代码前，请运行：

```bash
# Lint 检查
pnpm run lint

# 自动修复 lint 错误
pnpm run lint:fix

# 格式化代码
pnpm run format
```

### TypeScript 规范

- 使用 TypeScript 进行类型检查
- 避免使用 `any` 类型（ESLint 会警告）
- 为函数参数和返回值添加类型注解
- 使用接口和类型定义来定义数据结构

## 🧪 测试

### 添加测试

我们鼓励为代码添加测试：

```bash
# 运行测试（待实现）
pnpm run test

# 运行测试覆盖率（待实现）
pnpm run test:coverage
```

### 测试结构

```
src/
├── component/
│   ├── Button.tsx
│   └── Button.test.tsx  # 单元测试
├── containers/
│   └── Home.test.tsx    # 集成测试
└── __tests__/
    └── e2e/            # E2E 测试（待实现）
        └── home.spec.ts
```

## 📤 提交 Pull Request

### 1. 确保您的代码是最新的

```bash
# 获取上游更改
git fetch upstream

# 合并上游更改
git merge upstream/main
```

### 2. 推送到您的 fork

```bash
git push origin feature/your-feature-name
```

### 3. 创建 Pull Request

1. 访问 https://github.com/erishen/nsbp
2. 点击 "New Pull Request"
3. 选择您的分支
4. 填写 PR 模板
5. 等待代码审查

### PR 检查清单

在提交 PR 之前，请确保：

- [ ] 代码通过所有 lint 检查
- [ ] 代码符合项目规范
- [ ] 添加了必要的测试
- [ ] 测试全部通过
- [ ] 更新了相关文档
- [ ] 提交信息遵循 Conventional Commits 规范
- [ ] 没有合并冲突
- [ ] 清晰描述了更改内容

## 🐛 报告 Bug

如果您发现了 Bug，请创建一个 Issue：

1. 访问 https://github.com/erishen/nsbp/issues
2. 使用 "Bug report" 模板
3. 提供尽可能详细的信息：
   - Bug 描述
   - 复现步骤
   - 预期行为
   - 实际行为
   - 环境信息（OS、Node.js 版本、NSBP 版本）
   - 截图或日志

## 💡 提出功能建议

如果您有新功能的想法：

1. 检查现有的 Issues，避免重复
2. 使用 "Feature request" 模板创建 Issue
3. 清晰描述功能和动机
4. 如果可能，提供实现方案或伪代码

## 📚 改进文档

如果您发现文档问题：

1. 使用 "Documentation improvement" 模板创建 Issue
2. 指明需要改进的文档部分
3. 提供改进建议
4. 如果愿意，可以提交 PR 来改进文档

## 🌐 国际化

NSBP 目前主要支持中文文档。如果您愿意帮助：

- 翻译 README.md 为英文
- 翻译文档为其他语言
- 改进现有翻译

## 🎯 开发优先级

当前的开发优先级：

### 高优先级
- [ ] 添加单元测试和 E2E 测试
- [ ] 创建英文文档
- [ ] 改进错误消息
- [ ] 性能优化

### 中优先级
- [ ] 添加更多示例
- [ ] 创建视频教程
- [ ] 开发插件系统
- [ ] 添加更多模板

### 低优先级
- [ ] 创建官方文档网站
- [ ] 开发开发工具 CLI
- [ ] 社区功能集成

## 📧 联系方式

如果您有任何问题：

- 创建 GitHub Issue
- 发送邮件: erishen@qq.com
- 查看在线演示: https://nsbp.erishen.cn/

## 📄 许可证

通过贡献代码，您同意您的贡献将在与项目相同的 [MIT 许可证](LICENSE) 下发布。

## 🙏 感谢

感谢您对 NSBP 项目的贡献！每一个贡献，无论大小，都非常重要。

---

如果您在贡献过程中遇到任何问题，请随时创建 Issue 或联系我们。
